# holdings.json lives in the private stock-alert-agent-data repo,
# checked out into data/ by the workflows. Never commit it here.
data/
holdings.json

# Python bytecode, created by running tests/checks in a local clone.
__pycache__/
*.pyc
